return {
  battery = require("widgets.tray.battery"),
  network = require("widgets.tray.network"),
  updater = require("widgets.tray.updater"),
  volume = require("widgets.tray.volume"),
  layoutSwitcher = require("widgets.tray.layout-switcher"),
  systray = require("widgets.tray.systray")
}
